<form method="post" action="{{ path('theme_salle_delete', {'id': theme_salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme_salle.id) }}">
    <button class="btn">Delete</button>
</form>
