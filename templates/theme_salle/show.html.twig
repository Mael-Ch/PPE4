{% extends 'base.html.twig' %}

{% block title %}ThemeSalle{% endblock %}

{% block body %}
    <h1>ThemeSalle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ theme_salle.id }}</td>
            </tr>
            <tr>
                <th>Salle</th>
                <td>{{ theme_salle.salle }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ theme_salle.theme }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ theme_salle.dateDebut ? theme_salle.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ theme_salle.dateFin ? theme_salle.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('theme_salle_index') }}">back to list</a>

    <a href="{{ path('theme_salle_edit', {'id': theme_salle.id}) }}">edit</a>

    {{ include('theme_salle/_delete_form.html.twig') }}
{% endblock %}
